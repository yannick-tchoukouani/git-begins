Les commandes qui permettent de : 

- cloner un repository: 
	git clone https://github.com/yannick-tchoukouani/git-begins.git

- push, il y'en a 3 : 
	git add.
	git commit -m "Message du commit"
	git push origin master

La commande qui permet de pull est la suivante : 
	git pull origiin master 

La commande qui permet de créer une nouvelle branche : 
	git branch add-create-branch-command

La commande pour ensuite basculer vers celle-ci : 
	git checkout add-create-branch-command
